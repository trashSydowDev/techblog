web:
  build: .
  volumes:
    - .:/var/www/html
  ports:
    - "8000:80"
  links:
    - db
db:
  image: mysql:5.7
  volumes:
    - /tmp/dockermysql:/var/lib/mysql
  ports:
    - "3306:3306"
  environment:
    MYSQL_ALLOW_EMPTY_PASSWORD: yes
    MYSQL_DATABASE: techblog
